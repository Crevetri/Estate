import { PropertyRepository } from './property-repository';

describe('PropertyRepository', () => {
  it('should create an instance', () => {
    expect(new PropertyRepository()).toBeTruthy();
  });
});
